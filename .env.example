# LLM (set at least one)
OPENAI_API_KEY=
GEMINI_API_KEY=
LLM_PROVIDER=gemini
GEMINI_MODEL=gemini-2.5-flash

# Email automation (optional — logs still run without it)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
NOTIFY_EMAIL=

# Admin dashboard password (default: admin123)
ADMIN_PASSWORD=admin123
